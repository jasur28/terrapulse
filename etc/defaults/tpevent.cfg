# tpevent — anomaly-event grouping (shipped; do not edit).
# Override in etc/tpevent.cfg.

# An open event closes (RESOLVED) after this many seconds with no new anomaly
# from the structure. Longer = fewer, larger events.
event.quietSec = 15
